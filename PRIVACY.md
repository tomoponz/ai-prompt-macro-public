# プライバシー（実装の詳細）

利用者向けの[公開プライバシーポリシー](https://tomoponz.github.io/ai-prompt-macro/privacy.html)では、データの取り扱いと利用者による管理方法を説明しています。この文書では、その実装の詳細を説明します。

AI Prompt Macroは、ChatGPTの回答本文を読まない **Output-Blind** の設計です。

## 回答本文

拡張機能は、ChatGPTの回答本文を取得・保存・解析・送信・書き出ししてはならず、回答本文をワークフローの判断に使ってもなりません。

ChatGPT上で動くcontent scriptは、プロジェクトの設計により、次の対象だけに限定しています。

- 指示の入力欄（prompt composer）
- 送信ボタン（Send）
- 停止・生成状態の操作部品（Stop / generation control）
- 利用上限、CAPTCHA、ログイン要求、サービスエラーの検出に必要な、限定された状態表示・警告・ダイアログのUI
- ページURLから得る会話の識別情報

回答メッセージを指すselectorで要素を検索したり、回答内容にobserverを設定したりしません。

## ローカルデータ

`chrome.storage.local`でローカルに保存する情報:

- 利用者が作成した指示とワークフロー
- タブごとのワークフロー編集設定
- ワークフロー設定（指定時刻と、任意のスリープ防止設定を含む）
- タブ単位の実行中Runのcursorと状態
- 永続化した送信記録（durable outbox）内の、指示のハッシュと送信状態
- 上限付きの診断イベントの種別とタイムスタンプ
- 指定時刻のワークフローを起動するために必要なアラームのメタデータ（`runId`、`stepId`、予定・発火のタイムスタンプ、対象のタブID）

診断情報には、指示本文も回答内容も保存しません。

## 複数タブの扱い

サイドパネルは、開いている`chatgpt.com`のタブを一覧表示できます。これは、Start / Pause / Resume / Stop（開始・一時停止・再開・停止）の操作をどのタブへ送るかを、利用者が明示的に選べるようにするためです。

この目的で、拡張機能は次の情報を使います。

- ブラウザのタブID
- 該当するChatGPTタブがアクティブかどうか
- content scriptが返す、上限付きのMacro状態
- 安全確認の層ですでに使用している会話の識別情報

現在のChatGPTのページタイトルは、サイドパネルで人が見分けるための一時的なメタデータとしてだけ使います。既存の`https://chatgpt.com/*`のホスト権限で、該当するタブの`chrome.tabs.Tab.title`を読み取れるため、`tabs`権限は要求しません。タイトルは正規化し、最大150 Unicodeコードポイントに制限します。タイトルを、タブ・会話・Run・操作・leaseを決める根拠（authority）として使うことはなく、`chrome.storage.local`やsession storageへ書き込むことも、outbox・Run・診断情報の記録に含めることも、発行元（publisher）へ送信することもありません。利用者が入力する表示名（alias）は、これとは別の、ブラウザセッション中だけの設定です。回答本文は読み取りません。

サイドパネルは、表示専用の「表示中」マークのために、自身のウィンドウIDと、各タブのウィンドウIDおよびアクティブ状態を照合します。これは、明示的に選択した操作対象とは独立しています。タイトルやウィンドウのメタデータを取得できなくても、選択中の操作対象は変わらず、操作の根拠が与えられることもありません。

実行中Runの保存はタブIDごとに分かれているため、異なる2つのChatGPTタブが1つの共通の実行中Run枠を共有することはありません。2つのタブが同じ会話への送信者になろうとした場合に安全側で停止（fail closed）できるよう、会話単位のlease（conversation-scoped lease）も維持しています。

## スリープ防止（Keep Awake）

任意の **「スリープ防止」**（Keep Awake While Running）設定は、Chrome / Edgeの`power`権限を使います。

スリープ防止を有効にしたRunが1つ以上`running`の間、バックグラウンドのservice workerは`system`レベルのスリープ防止を要求します。これはシステムのスリープを防ぎますが、画面の消灯は妨げません。スリープ防止を有効にしたRunで実行中のものがなくなると、要求を解除します。

拡張機能は、すでにスリープ・休止状態・電源オフになっているPCを起こすためにpower APIを使いません。それには、この拡張機能の外側にあるOSレベルのスケジュール設定が必要です。

## ネットワークとテレメトリ

拡張機能の構成:

- バックエンドなし
- テレメトリなし
- アナリティクスなし
- リモートコードなし
- `webRequest`なし
- cookie・セッション情報の抽出なし
- 会話の書き出しなし

ネットワーク通信は、利用者のブラウザが行う通常のChatGPTページの通信だけです。`chrome.alarms`と`chrome.power`はブラウザ内のローカルAPIで、プロジェクトのバックエンドへ接続しません。

## 重要な制限

`chatgpt.com`上で動くcontent scriptは、技術的にはそのページのDOMへアクセスできます。そのためOutput-Blindは、ブラウザが強制する権限の境界ではなく、**監査可能な実装上の保証**です。このリポジトリのテストは、禁止した権限・API・selectorを検査し、違反をソースレビューとCIで見つけられるようにしています。

## Edge / Chromeでの自己修復のための注入

拡張機能は、`scripting`権限と、`https://chatgpt.com/*`だけを対象とするホストアクセスを要求します。通常の宣言済みcontent scriptが注入されていない場合、バックグラウンドのservice workerは、同梱された同じローカルスクリプトを明示的に選択したChatGPTタブへ注入し、接続を再試行することがあります。他のoriginへは注入せず、`<all_urls>`も要求しません。
