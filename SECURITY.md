# 安全性と信頼性のモデル

## 基本原則

指示の送信は冪等（idempotent）ではありません。同じ指示を重複して送信すると、ChatGPTに別のターンが作られます。

そのためAI Prompt Macroは、推測で続行するより停止することを優先します。

## ページタイトルは表示専用

正規化したChatGPTタブのタイトルは、一時的なタブ一覧の応答（ephemeral tab-list envelope）に含めてサイドパネルに表示することがあります。タイトルは、Run・outbox・診断情報・session / local storage・会話の識別情報（conversation identity）・leaseのキーのいずれにも入りません。現在のウィンドウでアクティブかどうかは表示用のメタデータであり、すべての操作は明示的に選択したタブに結び付いたままです。ホスト権限と、回答本文を読まないOutput-Blindの境界は変わりません。

## 送信プロトコル

1. 会話単位のlease（conversation-scoped lease）を取得する。
2. 操作部品の状態から、入力欄が待機状態（idle）であることを確認できるまで待つ。
3. 入力欄に利用者の下書きがすでにある場合は停止する。
4. 指示のSHA-256ハッシュを含むoutbox記録を永続保存する。
5. 指示を書き込む。
6. 入力欄のテキストが完全に一致することを検証する。
7. 送信ボタン（Send）を正確に1回だけクリックする。
8. outboxを`submitted`にする。
9. 入力欄がクリアされ、生成状態の操作部品が現れた場合にだけ、送信を確認済み（confirmed）とする。
10. 操作部品が安定期間（stability window）のあいだ準備完了の状態を保つまで待つ。
11. ワークフローのcursorを進める。

outboxが`prepared`または`submitted`の間にブラウザが再読み込みされた場合、送信結果は曖昧（ambiguous）とみなし、自動再試行を禁止します。

New Chat（新しいチャット）には、より限定された確認の経路が1つあります。最初のSendを1回クリックした後、Runを`new-chat-confirmation-required`として永続的に一時停止します。ルートの変更、再読み込み、履歴のナビゲーション（戻る / 進む）、プログラムによるナビゲーションでは、正規の会話（canonical conversation）を自動で採用しません。現在検証済みの正規の会話で、利用者が明示的に **Resume（再開）** した場合だけ、その会話を採用し、すでにクリック済みの最初のSendを正確に1回として数えます。

この候補版でも、New Chatの手動確認の境界は引き続き必要です。同じdocument内でプログラムによってルートが切り替わった場合にも、最初のSendで作られたルートと同じく、canonical URL、クリアされた入力欄、生成状態の操作部品という証拠がそろうことがあります。`userInitiated === false`のナビゲーションイベントは、どの操作がその会話を作ったかを証明しません。自動採用やleaseの移行は追加していません。

## 送信回数の上限

送信回数の上限を最終的に判断するのはcontent runtimeです。Runを作成する前に、予定送信数を厳密に再計算し、`maxSends`と送信ステップの`repeat`の値が指定されている場合は`1..50`の有限の整数であること、さらに`plannedSends <= maxSends <= 50`であることを必須とします。省略された値には上限付きの既定値を使います。明示的に指定された不正な値は、範囲内へ丸める（clamp）のではなく拒否します。

同じ上限を、送信処理に入る時点と、取り消せないクリックの直前にも再検証します。永続保存されたRunが不正な場合は`send_budget_invalid`で再開不可の一時停止にし、有効な上限を使い切った場合は`max_sends`で停止します。どちらの場合もSendをクリックしません。

## 停止要因

runnerは次の場合に一時停止します。

- 利用上限
- CAPTCHA / 人間であることの確認
- サービスエラー
- ログインが必要
- 完了待ちのタイムアウト
- タブ間のlease競合
- 送信または復旧の結果が曖昧な場合

これらの状態を回避・突破しようとはしません。

## 停止の意味

**Stop Macro**（マクロの停止）は、以降の指示を送らないようにします。ChatGPT自体の生成停止（Stop Generation）の操作部品は、意図的にクリックしません。

## 指定時刻の実行

`Wait Until`（指定時刻まで待つ）は、`chrome.alarms`と、ローカルに永続保存した予定のメタデータを使います。アラームは起動のきっかけにすぎず、ChatGPTの制限を回避することも、隠れたセッションを開くこともありません。

予定より遅れた場合の扱いは、明示的に決まっています。

- 既定: 遅れが設定した猶予時間を超えた場合は、一時停止して利用者の確認を待つ。
- 任意: 遅れて実行する。
- 任意: スキップする。

スリープ中や電源オフのPCを、拡張機能が強制的に起こすことはありません。同じブラウザの実行セッションが開いたままで、OSの復帰後に期限を過ぎたアラームを受け取った場合は、次の指示の操作より前に、遅延時のポリシーを適用します。

Chrome / Edgeの完全な再起動は、復旧の経路ではなく安全上の境界です。再起動すると新しい実行セッションを作成し、以前の実行中Runを再開不可として隔離（quarantine）し、それらの予定を解除し、スリープ防止（Keep Awake）を解放します。再起動後に、以前のセッションから指示が自動送信されることはありません。

`Draft only`（入力だけして確認）の指示ブロックは、空の入力欄へ書き込み、テキストが完全に一致することを検証し、ブロックのcursorを進めて一時停止します。Sendはクリックしません。

## content scriptの自己修復のための接続

Edge / Chromeとの互換性のため、通常のcontent scriptの受信側が存在しない場合、拡張機能は`https://chatgpt.com/*`に限って`chrome.scripting.executeScript`を使うことがあります。最初に接続先タブのoriginを確認し、広範なホスト権限は要求しません。
